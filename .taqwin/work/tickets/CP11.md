# CP11 — KNEZ Backend Feature Coverage & Control App Integration (PROMPT-011)

Rules:
- No simulations. No assumptions.
- Each ticket must be implemented and observable from Control App.
- Status must progress: TODO → IN_PROGRESS → VERIFIED.
- Each ticket requires an automated test (pytest and/or TestRunner/Playwright) + a manual scenario.

## Ticket Set (CP11-1 → CP11-15)

### CP11-1: Persistent Memory Store
- Status: VERIFIED
- Type: Backend ↔ Frontend
- Description:
  - Replace in-memory memory store with durable SQLite/SQLAlchemy store.
  - Ensure `GET /memory` and `GET /memory/{id}` reflect persisted data.
- Acceptance:
  - Create a memory record.
  - Restart KNEZ and confirm the record is still returned.
- Tests:
  - Backend: add pytest for persistence round-trip.
  - Frontend: Playwright scenario verifies memory appears in Memory Explorer.

### CP11-2: Persistent Knowledge Base
- Status: VERIFIED
- Type: Backend ↔ Frontend
- Description:
  - Replace in-memory knowledge base with durable SQLite store.
  - Ensure `POST /memory/knowledge` persists and `GET /memory/knowledge` returns stable ordering.
- Acceptance:
  - Add a KB entry, restart KNEZ, confirm it remains.
- Tests:
  - Backend: pytest persists KB entries.
  - Frontend: Playwright adds + reloads KB view.

### CP11-3: Memory Gate Violation Visualization
- Status: VERIFIED
- Type: Backend ↔ Frontend
- Description:
  - Surface memory gate rejections in Control App.
  - Show policy name, rule, timestamp, and denied content hash in UI.
- Acceptance:
  - Trigger a memory write that violates gate.
  - UI shows denial reason with evidence fields.
- Tests:
  - Backend: pytest that denial emits an event with payload.
  - Frontend: Playwright triggers denial and asserts UI rendering.

### CP11-4: Replay Correctness vs Event Stream
- Status: VERIFIED
- Type: Backend ↔ Frontend
- Description:
  - Ensure replay output is derived from stored events/checkpoints.
  - Validate ordering and event completeness for a session.
- Acceptance:
  - Generate a multi-turn session and fetch `/events` and `/sessions/{id}/replay`.
  - Replay contains the same turn boundaries as events.
- Tests:
  - Backend: pytest compares replay structure vs event store.

### CP11-5: Replay UI Playback with Checkpoints
- Status: VERIFIED
- Type: Frontend
- Description:
  - Add a replay playback UI with step-through checkpoints.
  - Show timestamps, event types, and message snapshots.
- Acceptance:
  - User can scrub through checkpoints and view corresponding content.
- Tests:
  - Frontend: Playwright loads replay and steps through checkpoints.

### CP11-6: Influence Denial Surface
- Status: VERIFIED
- Type: Backend ↔ Frontend
- Description:
  - Emit explicit events when influence is disabled/denied for a route decision.
  - Display influence denials in Governance panel and Events panel.
- Acceptance:
  - Disable influence, perform a chat turn, observe denial event and UI.
- Tests:
  - Backend: pytest denial event emission.
  - Frontend: Playwright verifies UI shows denial.

### CP11-7: Approval Workflow (Observable + Actionable)
- Status: VERIFIED
- Type: Backend ↔ Frontend
- Description:
  - Implement real `/approvals/pending` and `/approvals/{id}/decision` backed by durable store.
  - Add a Control App Approvals UI to list and approve/deny.
- Acceptance:
  - Create an approval-required action; see it in UI; approve; verify audit trail.
- Tests:
  - Backend: pytest approval queue + decision flow.
  - Frontend: Playwright approves an item and asserts status changes.

### CP11-8: Router Decision Trace
- Status: VERIFIED
- Type: Backend ↔ Frontend
- Description:
  - Make router scoring and selection trace observable (event payload or dedicated endpoint).
  - Add UI trace view (selected backend, scores, influence applied, memory hints count).
- Acceptance:
  - Perform a chat completion and view routing trace in UI.
- Tests:
  - Backend: pytest ensures routing trace schema.
  - Frontend: Playwright asserts trace visible after message.

### CP11-9: Model Routing Visibility
- Status: TODO
- Type: Backend ↔ Frontend
- Description:
  - Expose model backend selection and latency metrics per request.
  - Show in message metadata in Chat view.
- Acceptance:
  - Send a message; UI shows selected backend id and TTFT/latency.
- Tests:
  - Frontend: TestRunner validates metadata fields present.

### CP11-10: Event Causality Chain
- Status: TODO
- Type: Backend ↔ Frontend
- Description:
  - Add causal linkage fields to events (cause_event_id, parent_task_id).
  - Render chains in Control App events panel.
- Acceptance:
  - Trigger a chat completion; UI shows causality chain linking router → generation → memory writes.
- Tests:
  - Backend: pytest ensures causality fields are populated.
  - Frontend: Playwright asserts chain rendering.

### CP11-11: Sessions Metadata + Full History UX
- Status: TODO
- Type: Backend ↔ Frontend
- Description:
  - Add session naming/tags in backend and endpoints to read/update.
  - UI: list sessions with name, updated time, resume/new/analyze actions.
- Acceptance:
  - Rename session; refresh app; name persists; resume works.
- Tests:
  - Backend: pytest session metadata persistence.
  - Frontend: Playwright rename + resume.

### CP11-12: Failover Observability + Trigger
- Status: TODO
- Type: Backend ↔ Frontend
- Description:
  - Add an explicit failover status endpoint and event emission for triggers.
  - Add UI status and a safe manual trigger control.
- Acceptance:
  - Trigger failover; UI shows transition and resulting backend selection.
- Tests:
  - Backend: pytest failover emits expected events.
  - Frontend: Playwright validates status.

### CP11-13: Telemetry Metrics Endpoint + UI Summary
- Status: TODO
- Type: Backend ↔ Frontend
- Description:
  - Expose Prometheus `/metrics` endpoint from KNEZ.
  - Add a Control App Telemetry view for key counters (failures, ttft safeguard, requests).
- Acceptance:
  - `/metrics` responds; UI fetches and displays key metrics.
- Tests:
  - Backend: pytest `/metrics` returns 200 and includes expected series names.

### CP11-14: MCP Registry Enablement + Execution Boundaries
- Status: TODO
- Type: Backend ↔ Frontend
- Description:
  - Replace MCP hard-404 with a real registry endpoint.
  - Enforce boundaries: allowlist tools; log execution attempts to events.
- Acceptance:
  - Toggle MCP item in UI; backend persists state; events show boundary enforcement.
- Tests:
  - Backend: pytest registry read/write and boundary rules.
  - Frontend: Playwright toggles and sees state reflected.

### CP11-15: Shadow Subsystem Delta Visibility
- Status: TODO
- Type: Backend ↔ Frontend
- Description:
  - Implement shadow simulator to produce deltas (policy/influence impacts).
  - Display deltas in Governance/Shadow UI view.
- Acceptance:
  - Trigger a scenario with influence; UI shows a shadow delta entry.
- Tests:
  - Backend: pytest delta append + retrieval.
  - Frontend: Playwright asserts delta visible.
