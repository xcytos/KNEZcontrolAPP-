# Plan: Frontend-Truth Orchestration Repair (Checkpoint 1.81)

The "transformCallback" error confirms a critical version mismatch between the frontend Tauri bindings and the Rust backend plugins. We will resolve this by synchronizing dependencies, enforcing frontend environment checks, and correcting the governance record.

## Phase E: Governance & Self-Correction (Immediate)

1. **Log Mistake**: Record in `.taqwin/memory/log.md` that Checkpoint 1.8 was falsely verified via CLI-only proof.
2. **Update Status**: Mark Checkpoint 1.81 as **ACTIVE / BLOCKED** in `.taqwin/work/active.md`.
3. **Task Graph**: Add "Frontend-Truth Orchestration Repair" node.

## Phase A: Frontend-First Truth Audit

1. **Instrument** **`SystemPanel.tsx`**:

   * Add explicit logging in `startStack`:

     ```typescript
     console.log("[SystemPanel] Check:", {
       isTauri: !!window.__TAURI__,
       hasShell: !!Command
     });
     ```

   * Add Guard Clause: If running in browser (non-Tauri context), show "Shell unavailable in web mode" and abort.

   * Fix memory leaks by handling `unlisten` functions.

## Phase B: Core Fix (Dependency Synchronization)

1. **Rust Backend**: Run `cargo update` in `src-tauri/` to unlock `tauri-plugin-shell` from its older version and match the frontend's `v2.3.5`.
2. **Clean Slate**: Delete `src-tauri/target` to force a clean recompilation of the plugin.
3. **Frontend**: Re-run `npm install` to ensure strict version alignment.

## Phase C: Real Tests

1. **Update** **`SystemPanel.test.tsx`**:

   * Add test: **"Blocks execution in web context"** (Mock `window.__TAURI__ = undefined`).

   * Refine test: **"Attempts spawn in Tauri context"**.

## Phase D: Verification (Manual Proof Required)

1. Run `npm run tauri dev`.
2. **User Action Required**: You must click "Start Local Stack" in the Tauri window.
3. <br />

   ## **Success Signal**:

