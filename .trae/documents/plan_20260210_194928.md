I will perform a full clean build and run the E2E tests as requested.

### **Plan**

1. **Clean Cargo Artifacts**:

   * Run `cargo clean` inside the `src-tauri` directory to remove all Rust build artifacts.

2. **Clean Node Modules**:

   * Delete the `node_modules` directory to ensure a fresh dependency installation.

3. **Reinstall Dependencies**:

   * Run `npm install` to fetch fresh node modules.

4. **Build Application**:

   * Run `npm run build` to compile the frontend and ensure the TypeScript/Vite build is valid.

5. **Run Playwright Tests**: and ensure these are ui and click based if not update them  to show correct and actual tauri app , no mock , no simuation , no placehodlers

   * Run `npm run e2e:tauri`.

   * **Reason**: The previous `npx playwright test` failure (`net::ERR_CONNECTION_REFUSED`) occurred because the Tauri application wasn't running. The `e2e:tauri` script (`tests/tauri-e2e/spawn-and-run.js`) handles the full lifecycle:

     1. Spawns the KNEZ backend.
     2. Spawns the Tauri application in dev mode with CDP enabled.
     3. Waits for both to be healthy.
     4. Runs the Playwright tests against the live application.

This process ensures a completely clean environment and correct test execution flow.
