I have analyzed the codebase and confirmed the current state of MCP integration. Here is the implementation plan to address all your requirements.

### **Part 3 Implementation Plan**

#### **A) Registry & Inspector Visibility**

**Goal:** Ensure `github_remote` and other local servers appear in the Registry even if not in the backend snapshot.

* **Refine** **`McpRegistryView.tsx`**:

  * Verify the merging logic (currently `localServers` are added if ID is missing from snapshot).

  * Explicitly ensure `github_remote` is not filtered out by any status checks.

  * Add visual indicators for "local config only" items.

#### **B) Add Server UI**

**Goal:** Provide a seamless "Add Server" experience without external prompts.

* **Enhance** **`McpInspectorPanel.tsx`**:

  * Verify the "+" button logic opens the `showAddServer` modal (already present).

  * Ensure the modal validates JSON and merges strictly into `servers` object.

  * Confirm templates for `github_remote` and `github_local` are correct and usable.

#### **C) In-App Inputs Vault**

**Goal:** Replace `window.prompt` with a secure, in-app input mechanism.

* **Verify** **`McpInspectorPanel.tsx`**:

  * The "Inputs Vault" is already implemented. I will review it to ensure it catches all `mcp_input_required` errors from `McpInspectorService`.

  * Ensure `McpInspectorService` throws the correct error code to trigger the UI.

#### **D) Robust HTTP Logging & Connection**

**Goal:** Make HTTP connections deterministic and debuggable.

* **Update** **`McpHttpClient.ts`**:

  * Add explicit logging for HTTP status, Content-Type, and `Mcp-Session-Id`.

  * Ensure error messages are redacted (e.g., hiding tokens) before logging.

  * Capture `Mcp-Session-Id` from headers and reuse it in subsequent requests.

* **Update** **`McpInspectorPanel.tsx`**:

  * Ensure "Test Connect" button performs a full `initialize` + `tools/list` cycle and reports exact failure reasons.

#### **E) Direct Stdio Execution (No PowerShell)**

**Goal:** Remove PowerShell dependencies for better reliability and performance.

* **Update** **`TaqwinToolsModal.tsx`**:

  * Rewrite `autoDetectTaqwinConfig` to use `cmd /c where python` and `fs.exists` (via Tauri plugin) to find the TAQWIN path, instead of hardcoding or relying on assumptions.

* **Update** **`KnezClient.ts`**:

  * Replace `healthViaPowerShell` and `postJsonViaPowerShell` with `cmd` + `curl` equivalents as the primary fallback mechanism.

#### **F) Playwright E2E Tests**

9 NO MOCKS , FULL SAME , ensure it wokrs like a ctual mcp host and server client , interaction and conenction . 

**Goal:** Verify the full flow including `github_remote` and UI interactions.

* **Create** **`tests/tauri-playwright/mcp-github.spec.ts`**:

  * **Mock Server**: Spawn the actual github mcp server using its config for either local or `github_remote`.

  * **Test Flow**:

    1. Open MCP Registry.
    2. Click "+" -> Add correct Server Config.
    3. Click "Inspect" -> "Connect".
    4. Verify "Inputs Vault" appears (mocking a required token).
    5. Enter token -> Verify "Tools List" populates.
    6. Call a tool -> Verify Traffic Logs.

#### **G) Fix "4 Fields" & CORS Errors**

**Goal:** Resolve the reported runtime errors.

* **Fix** **`KnezClient.ts`**:

  * Update `emitTaqwinResponse` and `emitEvent` to catch `fetch` errors (CORS) and fall back to `postJsonViaShell` (cmd/curl). This will bypass the browser CORS restrictions in Tauri.

  * Ensure timeouts are handled gracefully to avoid "mcp\_request\_timeout" hanging the UI.

### **Part 4 Verification**

* Run `npm test` (Unit Tests).

* Run `npm run e2e:tauri` (Playwright Suite, including new GitHub/Mock test).

* Manual check of "Inputs Vault" with a dummy server config.

