I will fix the connectivity issues, CORS errors, and missing UI functionality as follows:

1. **Fix CORS Errors (Dev Environment)**:

   * Update `vite.config.ts` to add a proxy configuration. This will forward `/taqwin`, `/mcp`, and `/health` requests from the frontend (port 5173) to the backend (port 8000), eliminating the CORS errors visible in the console during development.

2. **Fix MCP Timeouts**:

   * Modify `src/mcp/client/McpStdioClient.ts` to increase the `initialize` timeout from 2.5s to 15s. The current 2.5s timeout is too short for Docker-based servers (like `github_local`) or slower Python processes to start up.

3. **Fix "Toggle" Logic for Local Servers**:

   * Modify `src/features/mcp/McpRegistryView.tsx`'s `handleToggle` function. Currently, it tries to call the backend for *all* servers. I will update it to handle local servers (like `github_local`) by updating the local configuration instead of calling the backend.

4. **Add "Add MCP Server" UI**:

   * Update `src/features/mcp/McpRegistryView.tsx` to include an "Add Server" button.

   * Implement a modal that allows you to paste a JSON configuration for a new server.

   * Upon saving, this will persist the new server to the local configuration, making it appear in the list where you can then toggle it on.

5. **Verify**:

   * Run `npm run e2e:tauri` to ensure no regressions.

   * The Playwright tests passed previously because they use mocks; these changes target the *real* application runtime issues you observed.

