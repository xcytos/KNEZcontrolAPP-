# CP6.1: Observation, Automation & Truth Verification Layer

I will verify the KNEZ Control App by establishing a Playwright-based truth layer that correlates UI state with backend reality.

## Phase 0: Governance Update

* **Log Mistake**: Record the "False Confidence" mistake in `.taqwin/memory/mistakes.md`.

* **Update Active**: Set `.taqwin/work/active.md` to "CP6.1 — Observation & Automation Verification".

## Phase 1 & 3: Playwright Setup

* **Justification**: Playwright is chosen over Puppeteer for its superior Tauri support, deterministic waiting, and cross-context observation (UI + Backend correlation).

* **Action**:

  * Install `@playwright/test`.

  * Create `tests/playwright/playwright.config.ts` configured for local web server (Tauri web preview mode).

  * Since I cannot easily run a headed Tauri app in this headless env, I will target the Web Preview (`npm run preview` / `localhost:1420`) which fully exercises the React frontend + Mock/Real Client logic.

  * try and make tauri desktop app launches and create test on it also for activation , health etc

    <br />

## Phase 2: Observation Instrumentation

* **Frontend**: Inject `window.__KNEZ_OBSERVER__` in `App.tsx` or `main.tsx`.

  * `emit(event, data)`: For state changes (e.g., "status\_changed", "message\_sent").

  * Expose `window.__KNEZ_STATE__` for direct assertion of internal React state if needed.

## Phase 4: Required Test Suite implementation

I will create `tests/playwright/*.spec.ts` covering:

1. **Boot & Trust**: `launch_knez_and_verify_health()`
2. **Orchestration Idempotency**: `double_click_launch_check()`
3. **Chat Reality**: `send_message_and_verify_event_log()`
4. **Perception**: `capture_snapshot_and_verify_image_data()`
5. **MCP**: `toggle_mcp_and_verify_persistence()`
6. **Agent Loop**: `start_agent_task_and_observe_steps()`
7. **Failure Injection**: `simulate_backend_death_and_check_ui()`

## Phase 5 & 6: Verification & Truth Table

* Run tests.

* Generate `Truth Table` in `.taqwin/memory/log.md`.

* **Strict Rule**: If any test fails, I will NOT mark CP6.1 as complete until fixed.

## Phase 7 & 8: Conclusion

* If PASS: Commit and generate `NEXTPLAN.md` (CP7).

* If FAIL: Create fix tickets.

